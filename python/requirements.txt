# ── 聚合入口: 默认全部安装 (含 ML 重型依赖) ─────────────
# 只想快速体验 (跳过 ~5GB 模型下载):
#   pip install -r requirements-core.txt
# 完整能力 (marker-pdf + docling + BGE-Reranker):
#   pip install -r requirements.txt
-r requirements-core.txt
-r requirements-ml.txt
